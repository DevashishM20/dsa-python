from numpy import *
a=[1,8,6,2,5,4,8,3,7]
left=0
right=len(a)-1
maximum=0
while left<right:
    area = min(a[left], a[right]) * (right - left)
    if area > maximum:
        maximum=area
    if a[left]<a[right]:
        left+=1
    else:
        right-=1
print(maximum)
